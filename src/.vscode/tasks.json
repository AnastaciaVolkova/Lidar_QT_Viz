{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "my_build",
            "type": "shell",
            "command": "cd ${workspaceFolder}/../build && cmake -DCMAKE_BUILD_TYPE=Debug ../src && make",
            "options": {
                "cwd": "${workspaceFolder}/../build"
            },
            "problemMatcher":["$gcc"],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}